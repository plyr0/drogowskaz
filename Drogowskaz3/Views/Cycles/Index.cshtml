@model IEnumerable<WebApplication1.Cycle>

@{
    ViewBag.Title = "Okresy";
}

<h2>Lista okresów</h2>

<p>
    @Html.ActionLink("Delete", "Delete")
</p>
<table class="table">
    <tr>
        <th>
            Nazwa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
    </tr>
}

</table>
